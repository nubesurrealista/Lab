name: Build Tuta Mail Desktop for Alpine Latest (Linker & Target Fix)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: 
      image: alpine:latest
      options: --user root

    steps:
    - name: Install System Dependencies
      run: |
        apk update
        apk add --no-cache \
          build-base git nodejs npm python3 py3-pip \
          bash libsecret-dev dbus-dev pkgconfig \
          libx11-dev libxkbfile-dev make g++ \
          openssh-client curl clang19 lld19 llvm19 binaryen doas \
          binutils binutils-gold

    - name: Configure doas
      run: |
        echo "permit nopass keepenv root as user" > /etc/doas.conf
        echo "permit nopass keepenv :root" >> /etc/doas.conf

    - name: Alpine Toolchain Symlinks (Total Fix)
      run: |
        # Enlaces prioritarios
        ln -sf /usr/bin/clang-19 /usr/bin/clang
        ln -sf /usr/bin/clang++-19 /usr/bin/clang++
        ln -sf /usr/bin/ld.lld /usr/bin/ld
        ln -sf /usr/bin/llvm-ar /usr/bin/ar
        ln -sf /usr/bin/llvm-nm /usr/bin/nm
        ln -sf /usr/bin/llvm-ranlib /usr/bin/ranlib

    - name: Setup Rustup (Multi-Target Alpine)
      run: |
        export HOME=/root
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable --profile minimal
        ln -sf /root/.cargo/bin/* /usr/bin/
        source /root/.cargo/env
        rustup target add x86_64-unknown-linux-musl
        rustup target add x86_64-unknown-linux-gnu
        rustup default stable-x86_64-unknown-linux-musl

    - name: "Emscripten: Bootstrap from Source"
      run: |
        git clone --depth 1 --branch 3.1.59 https://github.com/emscripten-core/emscripten.git $GITHUB_WORKSPACE/emscripten_src
        cd $GITHUB_WORKSPACE/emscripten_src
        cat <<EOF > .emscripten
        LLVM_ROOT = '/usr/bin'
        BINARYEN_ROOT = '/usr/bin'
        NODE_JS = '/usr/bin/node'
        EMSCRIPTEN_ROOT = '$GITHUB_WORKSPACE/emscripten_src'
        EOF
        cp .emscripten ~/.emscripten
        export LLVM_ROOT='/usr/bin'
        export PATH="/usr/bin:$PATH"
        ./bootstrap

    - name: Clone Tuta
      run: git clone https://github.com/tutao/tutanota.git

    - name: Checkout Latest Desktop Tag
      working-directory: ./tutanota
      run: |
        LATEST_TAG=$(git tag -l | grep "tutanota-desktop-release-" | sort -V | tail -n1)
        git checkout "$LATEST_TAG"

    - name: Initialize Submodules
      working-directory: ./tutanota
      run: git submodule update --init --recursive

    - name: Install Tuta NPM Dependencies
      working-directory: ./tutanota
      run: |
        git config --global url."https://github.com/".insteadOf git@github.com:
        npm ci --legacy-peer-deps

    - name: Build Tuta Packages (AWS-LC Linker Fix)
      working-directory: ./tutanota
      run: |
        export PATH="$GITHUB_WORKSPACE/emscripten_src:/usr/bin:/root/.cargo/bin:$PATH"
        export EMSCRIPTEN="$GITHUB_WORKSPACE/emscripten_src"
        export HOME=/root
        
        # DEFINICIÓN DE HERRAMIENTAS
        export CC=clang
        export CXX=clang++
        export AR=llvm-ar
        export NM=llvm-nm
        export RANLIB=llvm-ranlib
        
        # PARCHE CRÍTICO PARA AWS-LC-SYS
        # -fuse-ld=lld obliga a clang a usar lld sin buscar el binario 'ld' en rutas oscuras
        # Incluimos el fix de compatibilidad de headers de Alpine
        COMMON_FLAGS="-D__GNUC_PREREQ(maj,min)=((__GNUC__<<16)+__GNUC_MINOR__>=((maj)<<16)+(min)) -fuse-ld=lld"
        export CFLAGS="$COMMON_FLAGS"
        export CXXFLAGS="$COMMON_FLAGS"
        
        # Variables específicas para el build de Rust
        export CARGO_BUILD_TARGET=x86_64-unknown-linux-musl
        
        doas env PATH="$PATH" \
             HOME="$HOME" \
             CC="$CC" \
             CXX="$CXX" \
             CFLAGS="$CFLAGS" \
             CXXFLAGS="$CXXFLAGS" \
             CARGO_BUILD_TARGET="$CARGO_BUILD_TARGET" \
             npm run build-packages

    - name: Build Desktop Client (Unpacked)
      working-directory: ./tutanota
      run: |
        export PATH="$GITHUB_WORKSPACE/emscripten_src:/usr/bin:/root/.cargo/bin:$PATH"
        export HOME=/root
        export CC=clang
        export CXX=clang++
        export CFLAGS="-D__GNUC_PREREQ(maj,min)=((__GNUC__<<16)+__GNUC_MINOR__>=((maj)<<16)+(min)) -fuse-ld=lld"
        
        rm -rf libs/webassembly/include
        
        doas env PATH="$PATH" \
             HOME="$HOME" \
             CC="$CC" \
             CXX="$CXX" \
             CFLAGS="$CFLAGS" \
             node desktop --custom-desktop-release --unpacked

    - name: Prepare Export
      run: |
        mkdir -p export
        cp -r tutanota/build/desktop/* export/ 2>/dev/null || cp -r tutanota/dist/desktop/* export/ 2>/dev/null || echo "No files found"

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: tuta-desktop-alpine
        path: export/
