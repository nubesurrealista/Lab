name: Build Tuta Mail Desktop for Alpine Latest (Clang Fix)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: 
      image: alpine:latest
      options: --user root

    steps:
    - name: Install System Dependencies
      run: |
        apk update
        # Añadimos clang, lld y llvm
        apk add --no-cache \
          build-base git nodejs npm python3 \
          bash libsecret-dev dbus-dev pkgconfig \
          libx11-dev libxkbfile-dev make g++ py3-setuptools \
          openssh-client curl clang lld llvm

    - name: Setup Rust (Dual Target)
      run: |
        curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable --profile minimal
        echo "$HOME/.cargo/bin" >> $GITHUB_PATH
        source $HOME/.cargo/env
        
        # Necesitamos ambos targets para engañar a napi-rs
        rustup target add x86_64-unknown-linux-musl
        rustup target add x86_64-unknown-linux-gnu
        rustup default stable-x86_64-unknown-linux-musl

    - name: Install EMSDK
      run: |
        git clone https://github.com/emscripten-core/emsdk.git
        cd emsdk
        ./emsdk install 3.1.59
        ./emsdk activate 3.1.59

    - name: Clone Tuta
      run: git clone https://github.com/tutao/tutanota.git

    - name: Checkout Latest Desktop Tag
      working-directory: ./tutanota
      run: |
        LATEST_TAG=$(git tag -l | grep "tutanota-desktop-release-" | sort -V | tail -n1)
        echo "Versión Desktop detectada: $LATEST_TAG"
        git checkout "$LATEST_TAG"

    - name: Initialize Submodules
      working-directory: ./tutanota
      run: git submodule update --init --recursive

    - name: Install NPM Dependencies
      working-directory: ./tutanota
      run: |
        git config --global url."https://github.com/".insteadOf git@github.com:
        npm ci --legacy-peer-deps

    - name: Build Tuta Packages (Compiler Fix)
      working-directory: ./tutanota
      run: |
        # 1. Setup Entorno EMSDK
        cd ../emsdk && source ./emsdk_env.sh && cd ../tutanota
        
        # 2. Setup Rust
        source $HOME/.cargo/env
        
        # 3. EL PARCHE MAESTRO:
        # Usamos Clang en lugar de GCC
        export CC=clang
        export CXX=clang++
        export AR=llvm-ar
        
        # Definimos manualmente la macro que falta en Alpine (__GNUC_PREREQ)
        # Esto soluciona el error "missing binary operator"
        export CFLAGS="-D__GNUC_PREREQ(maj,min)=((__GNUC__<<16)+__GNUC_MINOR__>=((maj)<<16)+(min))"
        export CXXFLAGS=$CFLAGS
        
        # Variables de Rust para compilar estático (musl)
        export CARGO_BUILD_TARGET=x86_64-unknown-linux-musl
        export RUSTFLAGS="-C target-feature=-crt-static"
        
        npm run build-packages

    - name: Build Desktop Client (Unpacked)
      working-directory: ./tutanota
      run: |
        cd ../emsdk && source ./emsdk_env.sh && cd ../tutanota
        source $HOME/.cargo/env
        
        # Mantenemos las banderas para este paso también
        export CC=clang
        export CXX=clang++
        export CARGO_BUILD_TARGET=x86_64-unknown-linux-musl
        export CFLAGS="-D__GNUC_PREREQ(maj,min)=((__GNUC__<<16)+__GNUC_MINOR__>=((maj)<<16)+(min))"
        
        rm -rf libs/webassembly/include
        node desktop --custom-desktop-release --unpacked

    - name: Prepare Export
      run: |
        mkdir -p export
        cp -r tutanota/build/desktop/* export/ 2>/dev/null || cp -r tutanota/dist/desktop/* export/

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: tuta-desktop-alpine
        path: export/
